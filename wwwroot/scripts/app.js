// define the app's global variable
var app = app || {};

/* 
 attach to wsb the different components,
 in other words fake namespaces 
 */
app.data = {};
app.utils = {};
app.modules = {};
app.validations = {};